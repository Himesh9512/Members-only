extend layout 

block content   
    h1 Get Admin access

    p enter the secret code to become the admin of member's club! 

    br

    form(action="/admin", method="post")

        label(for="secret-code") Enter the Code: 
        input#secret-code(type="text", name="secret-code", required) 

        button(type="submit") Submit
    
    if errors 
        ul
            for error in errors 
                li!= error.msg
